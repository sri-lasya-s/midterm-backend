/**
 * UX Research Study Tracker API
 * API for managing UX research studies
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The StudyCreate model module.
 * @module model/StudyCreate
 * @version 1.0.0
 */
class StudyCreate {
    /**
     * Constructs a new <code>StudyCreate</code>.
     * @alias module:model/StudyCreate
     * @param studyTitle {String} 
     * @param method {module:model/StudyCreate.MethodEnum} 
     * @param participants {Number} 
     * @param status {module:model/StudyCreate.StatusEnum} 
     */
    constructor(studyTitle, method, participants, status) { 
        
        StudyCreate.initialize(this, studyTitle, method, participants, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, studyTitle, method, participants, status) { 
        obj['studyTitle'] = studyTitle;
        obj['method'] = method;
        obj['participants'] = participants;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>StudyCreate</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/StudyCreate} obj Optional instance to populate.
     * @return {module:model/StudyCreate} The populated <code>StudyCreate</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new StudyCreate();

            if (data.hasOwnProperty('studyTitle')) {
                obj['studyTitle'] = ApiClient.convertToType(data['studyTitle'], 'String');
            }
            if (data.hasOwnProperty('method')) {
                obj['method'] = ApiClient.convertToType(data['method'], 'String');
            }
            if (data.hasOwnProperty('participants')) {
                obj['participants'] = ApiClient.convertToType(data['participants'], 'Number');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>StudyCreate</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>StudyCreate</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of StudyCreate.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['studyTitle'] && !(typeof data['studyTitle'] === 'string' || data['studyTitle'] instanceof String)) {
            throw new Error("Expected the field `studyTitle` to be a primitive type in the JSON string but got " + data['studyTitle']);
        }
        // ensure the json data is a string
        if (data['method'] && !(typeof data['method'] === 'string' || data['method'] instanceof String)) {
            throw new Error("Expected the field `method` to be a primitive type in the JSON string but got " + data['method']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

StudyCreate.RequiredProperties = ["studyTitle", "method", "participants", "status"];

/**
 * @member {String} studyTitle
 */
StudyCreate.prototype['studyTitle'] = undefined;

/**
 * @member {module:model/StudyCreate.MethodEnum} method
 */
StudyCreate.prototype['method'] = undefined;

/**
 * @member {Number} participants
 */
StudyCreate.prototype['participants'] = undefined;

/**
 * @member {module:model/StudyCreate.StatusEnum} status
 */
StudyCreate.prototype['status'] = undefined;





/**
 * Allowed values for the <code>method</code> property.
 * @enum {String}
 * @readonly
 */
StudyCreate['MethodEnum'] = {

    /**
     * value: "Interview"
     * @const
     */
    "Interview": "Interview",

    /**
     * value: "Survey"
     * @const
     */
    "Survey": "Survey",

    /**
     * value: "UsabilityTest"
     * @const
     */
    "UsabilityTest": "UsabilityTest"
};


/**
 * Allowed values for the <code>status</code> property.
 * @enum {String}
 * @readonly
 */
StudyCreate['StatusEnum'] = {

    /**
     * value: "Active"
     * @const
     */
    "Active": "Active",

    /**
     * value: "Planned"
     * @const
     */
    "Planned": "Planned",

    /**
     * value: "Done"
     * @const
     */
    "Done": "Done"
};



export default StudyCreate;

